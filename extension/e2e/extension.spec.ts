import { test, expect } from '@playwright/test'

test('extension loads', async () => {
  // This is a placeholder. Real extension testing requires loading the extension context.
  // For now we just check if we can run a basic test.
  expect(true).toBe(true)
})
