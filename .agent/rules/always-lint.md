
After making any code changes, always run the project-wide lint check and address any errors before considering a task complete.

The monorepo uses pnpm and the root eslint, so the correct command is (from the repository root):

    pnpm lint

