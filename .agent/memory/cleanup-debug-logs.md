# Cleanup Debug Logs

Always clean up temporary debug log files (e.g., `*.log`, `*.txt`) created during investigation or testing before finishing the task.

Do not leave these files in the repository as they clutter the workspace and may be annoying to access if gitignored.
