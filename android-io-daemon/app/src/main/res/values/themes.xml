<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.JSTorrent" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Transparent theme for AddRootActivity (SAF folder picker launcher) -->
    <style name="Theme.JSTorrent.Transparent" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>