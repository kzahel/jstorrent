# JSTorrent Native Host Development Configuration
# Copy this file to ~/.config/jstorrent-native/jstorrent-native.env

# Enable logging to files (native-host.log, io-daemon.log, link-handler.log)
LOGFILE=true

# Override the launch URL for local development (used by link-handler when native-host is not running)
LAUNCH_URL=http://local.jstorrent.com:3000/launch/index.html

# Additional CORS origins for io-daemon (comma-separated)
# Allows the extension dev server (HMR) to connect to io-daemon
DEV_ORIGINS=http://local.jstorrent.com:3001,http://local.jstorrent.com:3000
