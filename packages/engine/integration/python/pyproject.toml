[project]
name = "jstorrent-python-tests"
version = "0.1.0"
description = "Python integration tests for JSTorrent engine"
requires-python = ">=3.10"
dependencies = [
    "requests",
    "libtorrent",
    "numpy",
]
