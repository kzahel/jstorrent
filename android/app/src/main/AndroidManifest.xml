<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Foreground service permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JSTorrent"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.JSTorrent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Pairing intent from extension -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="jstorrent" android:host="pair" />
            </intent-filter>

            <!-- Launch intent (just start app, don't modify pairing) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="jstorrent" android:host="launch" />
            </intent-filter>

            <!-- Magnet links -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="magnet" />
            </intent-filter>

            <!-- Torrent files via MIME type -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="application/x-bittorrent" />
            </intent-filter>

            <!-- Torrent files via file extension (content:// URIs) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" />
                <data android:host="*" />
                <data android:pathPattern=".*\\.torrent" />
            </intent-filter>

            <!-- Torrent files via file extension (file:// URIs) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="file" />
                <data android:pathPattern=".*\\.torrent" />
            </intent-filter>

        </activity>

        <!-- IO Daemon foreground service -->
        <service
            android:name=".service.IoDaemonService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- QuickJS engine foreground service -->
        <service
            android:name=".service.EngineService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- Pairing approval activity -->
        <activity
            android:name=".PairingApprovalActivity"
            android:exported="false"
            android:theme="@style/Theme.JSTorrent"
            android:excludeFromRecents="true" />

        <!-- SAF folder picker activity -->
        <activity
            android:name=".AddRootActivity"
            android:exported="true"
            android:theme="@style/Theme.JSTorrent.Transparent"
            android:excludeFromRecents="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="jstorrent" android:host="add-root" />
            </intent-filter>
        </activity>

        <!-- Standalone mode activity (non-Chromebook Android) - WebView based -->
        <!-- Note: External intents (magnet, torrent files) go through MainActivity which routes here -->
        <activity
            android:name=".StandaloneActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.JSTorrent"
            android:configChanges="orientation|screenSize|keyboardHidden">

            <!-- Internal intents -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="jstorrent" android:host="add-root" />
            </intent-filter>
        </activity>

        <!-- Native standalone activity (Compose UI with QuickJS engine) -->
        <activity
            android:name=".NativeStandaloneActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.JSTorrent"
            android:configChanges="orientation|screenSize|keyboardHidden">

            <!-- Direct launch intent -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="jstorrent" android:host="native" />
            </intent-filter>
        </activity>

    </application>

</manifest>
