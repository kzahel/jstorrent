<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Main app theme (dark mode) -->
    <style name="Theme.JSTorrent" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/splash_background_dark</item>
        <item name="android:statusBarColor">@color/splash_background_dark</item>
        <item name="android:navigationBarColor">@color/splash_background_dark</item>
    </style>

    <!-- Transparent theme for AddRootActivity (SAF folder picker launcher) -->
    <style name="Theme.JSTorrent.Transparent" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
